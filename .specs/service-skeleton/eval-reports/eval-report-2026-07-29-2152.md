# Specs self-evaluation — service-skeleton

Date: 2026-07-29-2152
Evaluated: `.specs/service-skeleton/{requirements,design,tasks}.md` (working-tree state on branch
`main`). `.specs/_eval-checklist.md` is absent, so the skill's nine-item fallback checklist was
used.

| # | Check | Verdict |
| - | ----- | ------- |
| 1 | Each AC is testable | PASS |
| 2 | Tasks have refs and DoD | PASS |
| 3 | Dependencies between tasks are explicit | PASS |
| 4 | Every AC in requirements.md is addressed by at least one section in design.md | PASS |
| 5 | Every AC traces to at least one task whose DoD would fail if the AC regressed | PASS |
| 6 | No contradictions between requirements.md, design.md, and tasks.md | PASS |
| 7 | Cross-references resolve | PASS |
| 8 | Every AC is in EARS form | PASS |
| 9 | Open questions are resolved | PASS |

---

## 1. Each AC is testable — **PASS**

The full walk found 57 ACs and an observable decision procedure for each. HTTP criteria specify
statuses, bodies, headers, or persisted state: AC1.1 requires *"`201 Created` with the created
representation and a `Location` header"*, while revised AC3.1 requires a *"non-null `content`
array with nested `page` metadata"*. Operational criteria are equally observable: AC9.4 requires
documented probes to report `UP`, AC9.5 distinguishes readiness from liveness during database
loss, and AC11.1 names the exact build lifecycle and check classes.

## 2. Tasks have refs and DoD — **PASS**

All ten task blocks contain one `Commit`, `Depends on`, `Refs.`, `Scope.`, and `DoD.` block. The
references identify both sides of the contract, such as T4's *"`requirements.md` AC3.1-AC3.8"*
and *"`design.md` §2.3, §3.3, §4.3, §5.1, §6, §11.2, §12"*. Definitions of done remain
executable: T4 requires the exact Spring Data `PagedModel` envelope *"without legacy aliases or
HATEOAS links"*, T6 requires captured server stack traces, and T9 owns live readiness/liveness
outage and recovery acceptance. Every task retains an explicit verification gate.

## 3. Dependencies between tasks are explicit — **PASS**

Section 2 contains a Mermaid dependency graph from `T1["T1 Build foundation"]` through
`T10["T10 Documentation and acceptance"]` and states, *"The graph is intentionally linear."*
Every task repeats its immediate prerequisite, from T1's *"Depends on: None."* to T10's
*"Depends on: T9."* The revised page representation stays within T4 and its T7 contract guard,
which matches the established delivery order.

## 4. Every AC in requirements.md is addressed by at least one section in design.md — **PASS**

The requirements index contains 57 unique AC ids; expanding the ranges in design §13 covers the
same 57 ids with no missing or extra id. The cited sections were checked against the AC text:

| AC group | Design section |
| --- | --- |
| AC1.1-AC1.4 | §2.3, §3.1, §4.3-§4.4, §5.1, §6 |
| AC2.1-AC2.2 | §3.2, §4.3, §6 |
| AC3.1-AC3.8 | §2.3, §3.3, §4.3, §5.1, §12 |
| AC4.1-AC5.3 | §3.4-§3.5, §4.4, §5.1-§5.3, §12 |
| AC6.1-AC6.6 | §2.1, §3.6, §6 |
| AC7.1-AC7.3 | §7 |
| AC8.1-AC8.6 | §4.1-§4.2, §8.1, §8.3, §11.2 |
| AC9.1-AC9.7 | §8.1-§8.2, §9, §11.4 |
| AC10.1-AC10.4 | §7.1, §8.1-§8.2, §10.3-§10.4 |
| AC11.1-AC11.6 | §10.1-§10.2, §11 |
| AC12.1-AC12.2 | §2.2 |

Design §2.3 now gives the complete `content` plus nested `page` example and §3.3 defines
past-the-end behavior. No orphan AC remains.

## 5. Every AC traces to at least one task whose DoD would fail if the AC regressed — **PASS**

Expanding the ranges in `tasks.md` produces the same 57 ids as the requirements index, with no
missing or extra id. AC3.1/AC3.8 map to T4's assertions for the non-null or empty `content` array,
nested page number/size/totals, and exact two-part envelope; AC3.4/AC3.7 map to its filtering and
invalid-query matrix. Other regression-sensitive examples remain AC6.6 through T6's sanitized
response and captured server stack trace, AC8.6 through T2's schema-isolation checks, and
AC9.4/AC9.5 through T8 configuration plus T9 live outage/recovery checks.

## 6. No contradictions between requirements.md, design.md, and tasks.md — **PASS**

The revised collection response has one answer everywhere: AC3.1 and AC3.8 name `content` and
nested `page`; design §2.3 explicitly selects
`org.springframework.data.web.PagedModel<InventoryItemDTO>` without HATEOAS, global `PageImpl`
serialization, or legacy aliases; and T4/T7 require runtime and generated-contract assertions for
that shape. Design §4.3 also maps entities to `InventoryItemDTO` before wrapping. Versions,
database/schema names, error fields, health ownership, and verification commands remain
consistent across all three documents.

## 7. Cross-references resolve — **PASS**

Every `§X.Y`, AC range, and prose `section X.Y` citation was checked against the heading and AC
indices. T4 points to design §2.3 and §3.3 for the page representation and query behavior; T7
points to §7.1-§7.3 for springdoc derivation and contract assertions. Requirements resolution 5
now points to §2.3 and §3.3 for the explicit `PagedModel` decision, while the existing health,
container, build, and traceability references still resolve to matching content.

## 8. Every AC is in EARS form — **PASS**

All 57 labeled ACs match their grammatical prefix: 17 Ubiquitous criteria begin with `The`, 21
Event-driven criteria begin with `When`, 16 Unwanted criteria begin with `If`, and three
State-driven criteria begin with `While`; there are no Optional criteria. Revised AC3.1 and AC3.8
remain Event-driven criteria beginning with `When`. No label/grammar mismatch was found.

## 9. Open questions are resolved — **PASS**

`requirements.md` has no live `## Open questions` heading and instead contains
`## Resolved questions` with 19 inlined answers. Resolution 5 now records that collection
retrieval uses the *"non-HATEOAS Spring Data `PagedModel<InventoryItemDTO>` shape with `content`
and nested `page` metadata"* and points to design §2.3 and §3.3. The remaining resolutions retain
their inlined answers and matching design or task pointers.

---

## Summary

No blocking or weak checklist item was found. The three specs define 57 testable EARS criteria,
retain complete design and task traceability, and consistently replace the custom page record
with an explicit typed Spring Data `PagedModel` whose runtime and OpenAPI shapes are guarded by
T4 and T7.
